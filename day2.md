day2 learnings

A skill is code that extends the model; the model requests, and the code executes.
The description controls tool behavior more than the implementation does.
Function calling is the planner to executor to memory loop made concrete.
Skills are swappable.provider differences are dialect, not concept.