Day1 Learnings

llms
the llms are large language models on which apps like chatgpt claude gemini run they can just work like this u give it an input and it uses the pretrained model and gives u the answer like u give input it uses llm and gives an output

aiworkflows
in ai workflows the ai follows a predefined path set by me u give it a path the ai uses that path and tells u what that path tells it for example if a user says use google calender and tell me what is the date of my doctor appointment it will check through google calender and give the answer this is ai workflow
in ai workflow user gives input and gets an output just like llm but it also provides a predefined path that helps the llm to answer the user query

aiagents
ai agent recieves a goal it performs reasoning how best to achieve the goal then do actions using the tools and then observes if iterations are required or not if so it iterates if not it gives the output.
an ai agent works autonomously in a loop it plans acts observes and repeats all by itself the loop is what make it different from the others for example a chatbot answers once but the agent keeps going again and again

